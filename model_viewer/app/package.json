{
  "name": "openscad-live-model-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Local STL and 3MF workspace viewer for the OpenSCAD model repository.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node --check src/main.js && node --check build.mjs"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
